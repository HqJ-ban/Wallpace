# 2026-08-11

## 打包成 exe 的环境坑（Wallpace / PyInstaller）

- 机器上有两套 Python：
  - PATH 第一个 = WorkBuddy 自带 `C:\Users\...\workbuddy\binaries\python\versions\3.13.12`（**缺 PySide6 / PyInstaller**）。
  - 系统 Python `C:\Programs\Python\Python313`（**已装齐 PySide6 6.11.1 + PyInstaller 6.21.0**）。
- 现象：双击 `build.bat` "没反应" → 因为脚本里用裸 `python`/`pip`，解析到 WorkBuddy 那套缺依赖的 Python。
- 修复：`build.bat` 顶部加了一行（仅脚本会话内生效，不动全局）：
  `set "PATH=C:\Programs\Python\Python313;C:\Programs\Python\Python313\Scripts;%PATH%"`
  之后裸 `python`/`pip`/`pyinstaller` 都指向系统 3.13，双击可正常打包。
- **关键坑**：系统 Python 下 `python -m pyinstaller` 报 "No module named pyinstaller"，但 `C:\Programs\Python\Python313\Scripts\pyinstaller.exe` 直接调用正常（6.21.0）。metadata 与包错位导致 `-m` 失效。**打包请直接用 pyinstaller.exe，别用 `python -m pyinstaller`**。
- 构建命令：`cd D:\my_project\wallpace && C:\Programs\Python\Python313\Scripts\pyinstaller.exe --name wallpace --windowed --onefile --clean src/main.py`，产物在 `dist\wallpace.exe`。`build.bat` 只打包不启动应用，要另外双击 `dist\wallpace.exe`。
- 运行源码：`C:\Programs\Python\Python313\python.exe src\main.py`。
- 测试（headless）：`QT_QPA_PLATFORM=offscreen python -m pytest -q`（用系统 Python 3.13 跑，103 passed / 1 skipped）。

## git 仓库损坏 object 修复（2026-08-11）

- 仓库对象库有损坏/缺失对象：**本地 clone 不完整，历史最早处断裂**。`fb9a859`（master 上最早可达 commit）的父是 `bc80c3ac`，其祖父 `0db1da95` 缺失；`git fsck` 还报大量 broken link / missing blob。
- **commit 失败现象**：`error: invalid object <hash> for 'src/...'` + `Error building trees`。根因是某些 tracked 文件的 blob 在 `.git/objects` 里损坏了（磁盘/沙箱写入导致），git add 因 stat 缓存沿用损坏指针不重写。
- **修复法（逐个暴露的损坏 blob）**：
  1. `rm -f .git/objects/<aa>/<剩余hash>`
  2. `git update-index --force-remove <path>` 再 `git add <path>`（强制重新 hash 工作区真实内容、写新 blob）
  3. 也可用 `git ls-tree -r HEAD` 配合 `git cat-file -e` 批量扫出所有损坏 blob 一次性处理。
  4. commit 时还会因"与父 commit 算 diff 读不到旧 blob"报 `fatal: unable to read ...`，但该 fatal 不影响新 commit 落地（新 tree 已用新 blob 写好）。
- 本次修复的损坏 blob：`src/app/widgets/__init__.py`、`src/app/image_loader.py`、`src/core/image_library.py`、`requirements.txt`。最终 commit = `c067348`（含 UI 卡死修复）。
- **`git gc --prune=now` 会失败**：traverse 时撞缺失父 `0db1da95` → `fatal: Failed to traverse parents of bc80c3ac` → repack 中断。历史断裂无法在不丢历史的情况下消除。
- **沙箱 `git fetch origin` 为空**：拉不到远程补全（网络限制或远程已重写历史）。
- 结论：当前 master 5 个 commit 完好可用（开发/打包/运行都不受影响），仅 `git fsck`/`git gc` 报警。彻底干净需联网后重新 `git clone` 再 cherry-pick 修复，或接受历史截断现状。

## 新功能：关闭→托盘常驻 + 开机自启开关（2026-08-11）

- 走了团队快速模式：TeamCreate(software-tray-autostart) → 工程师实现 → QA 验证。
- 实现本质是"接线"：前任早已写好 `AutostartManager`(src/core/autostart_registry.py)、`auto_start`/`minimize_to_tray` 配置项、托盘菜单，但没接上线。
- 改动文件：
  - `src/app/window.py`：`closeEvent` 尊重 `minimize_to_tray`（默认 True → 忽略关闭事件 + `hide()` + 保留托盘与调度器；`quit_app()` 带 `_force_quit` 强制退出）；`_build_settings_page` 新增"通用"分组含「开机自启」「关闭窗口时最小化到托盘」两个 QCheckBox。
  - `src/app/tray.py`：`_on_exit` 改调 `quit_app()`（否则被最小化逻辑拦截）。
  - `src/core/autostart_registry.py`：`enable()` 改为始终带 `--hidden`（开发/打包态开机自启都进托盘不弹主窗口）。
  - `src/main.py`：启动按 `auto_start` 与注册表状态一致性同步（仅不一致时写 HKCU\Run）。
- 测试：基线 103 → **110 passed / 1 skipped**（新增 7 项：关闭行为 3 + 自启开关 4）。代码库测试用 mock，不碰真实注册表。
- **QA 发现的真实缺陷已修**：`tests/test_autostart_registry.py` 有 4 个 `@pytest.mark.skipif(False,...)` 的 "no_crash" 测试会真写 `HKCU\Run`（开机自启污染本机），已改为 mock `winreg`，并清理了现场遗留的自启项。
- 测试命令：`QT_QPA_PLATFORM=offscreen C:/Programs/Python/Python313/python.exe -m pytest -q --basetemp="$TEMP/wallpace-pytest-<rand>"`（沙箱会拦截默认 basetemp 的 rmtree）。
- 注意：`tests/test_window.py` 或旧用例会改写被跟踪的 `.wallpace.json`（cwd 里 Settings.set），属既有行为，运行 pytest 后会污染该文件，需 `git checkout` 还原。

## 提交托盘/自启改动 + 清理 agents 引用（2026-08-11 晚）

- 提交：`df8bd4f` `feat: minimize-to-tray on close + autostart toggle in settings`，8 文件 +273/-32。含 QA 的注册表测试 mock 修复。
- **`.wallspace.json` 异常**：本次操作前该文件 index 条目莫名丢失、磁盘文件也消失（疑似测试/进程副作用）；HEAD 树里仍有该 blob(`177c067`)。`git checkout --/.wallspace.json` 因 Git Bash 对点文件名 pathspec 解析失败而报错；改用 `git cat-file -p <blob> > .wallspace.json` 还原内容再 `git add` 才成功。提交后该文件以干净状态(仅含 `D:/my_project/source picture`)回到仓库。
- **清理 `refs/agents/...` 检查点引用**：5 条 `refs/agents/7ea8815d-.../checkpoints/turn/0..4` 是 Agent host session 基线（非用户代码），用 `git for-each-ref --format='%(refname)' refs/agents/ | while read r; do git update-ref -d "$r"; done` 删除。注意 `git for-each-ref` 无匹配也返回 0，判断空需用 `$(...)` 捕获输出。删除后 fsck 的 broken link 报错未减少（那些是主历史线深处的 `0db1da95` 断裂，需联网 re-clone 才根治）。
- 工作区最终 clean；临时日志 `build_log.txt`/`run_out.txt` 已用绝对路径 `rm -f` 清掉（相对路径会被沙箱 safe-delete 拦截）。
- 现状：master 可达历史 6 个 commit(df8bd4f..fb9a859) 完好；`fb9a859` 父 `0db1da95` 缺失 → `git fsck`/`git gc`/`git rev-list --count` 仍报历史断裂，但不影响开发/打包/运行。

## git push 到 GitHub（2026-08-11 晚）

- `git push origin master` 成功：`2fa727c..df8bd4f master -> master`，2 个 commit(c067348 UI 卡死修复 + df8bd4f 托盘/自启) 已上 `origin/master`。
- 注意：push 能成是因为 remote URL 内嵌了 GitHub token(`https://ghp_***@github.com/HqJ-ban/Wallpace.git`)，所以即使 `git fetch` 曾空返回，push 仍可联网成功。该 token 暴露在 git remote 配置中，建议后续在 GitHub 端轮换或改用凭据管理器。
- push 后 `master...origin/master` 无 ahead/behind 标记，本地与远程同步。
- 未跟踪/未提交项：`.workbuddy/memory/2026-08-11.md`（本工作日志，本地记录用，未纳入本次 commit/push）。

## P0 修复 + 配置迁移 + 架构拆分（2026-08-11 晚）

- **用户现象**：开启开机自启后，之前改过的设置全部丢失。根因：旧 `Settings` 默认路径是 `Path.cwd()/.wallspace.json`（CWD 相关）；开机自启经注册表启动时 CWD=`C:\Windows\system32`，读不到项目里的配置 → 回退默认 → 看似"全没了"。
- **修复（src/core/settings.py）**：默认配置路径改为 `%APPDATA%/.wallpace/config.json`（`APP_DIR = Path.home()/".wallpace"`），与 CWD 无关；新增从旧 `.wallspace.json`（CWD / 项目根）的平滑迁移 `_try_migrate`；新增 `log_level` 配置项与 `set_many()`（单次落盘）。首次无默认配置时自动迁移，避免重复造默认。
- **收藏/跳过持久化**：`image_library.py` 新增 `load_persisted_state(skip_list, favorites)`；`window.py` 启动即还原，`_handle_skip`/`_handle_favorite` 后调用 `_persist_skip_favorites()`（写回 `skip_list`/`favorites`）；`main.py` 加 `faulthandler` 卡死 dump 到 `%APPDATA%/.wallpace/logs/traceback.log`，并按 `log_level`/`--debug` 调日志级别。
- **收藏 UI**：新建 `src/app/pages/gallery_page.py`（`GalleryPage`），左侧"图片库"整页提供 全部/收藏/已跳过 筛选 + 缩略图网格（★ 收藏、`恢复` 取消跳过），缩略图走 `image_loader` 异步解码。`window.py` page1 改为 `GalleryPage`；`_apply_scan_results` 扫描完成后也 `gallery.refresh()` 同步。
- **架构拆分（"拆分 window.py"）**：在 `gallery_page.py` 之外，又把设置页抽成 `src/app/pages/settings_page.py`（`SettingsPage`，视图层 + 纯 UI 辅助；业务逻辑经回调回 MainWindow）。保持 `AutostartManager`/Scheduler 引用仍在 `window.py`，以兼容既有测试（尤其对 `src.app.window.AutostartManager` 的 monkeypatch）。`window.py` 现只留逻辑回调，UI 构建在 pages 子包。
- **测试隔离（重要坑）**：`test_settings.py` 用 `from core.settings import ...`，`test_window.py` 用 `from src.core.settings import ...`——二者在 `sys.modules` 是**不同模块对象**。只 patch `src.core.settings.DEFAULT_CONFIG_PATH` 不够，`test_settings.py` 的 `Settings()` 仍会写真实 `%APPDATA%/.wallpace/config.json`（已踩坑：首次测试跑挂后污染了用户配置）。`conftest.tmp_config_dir` 现对 `("src.core.settings","core.settings")` **两个名字都**重定向 `DEFAULT_CONFIG_PATH` 与清空 `LEGACY_CONFIG_PATHS`；`main_window` fixture 也改为依赖 `tmp_config_dir`。
- **本机配置抢救**：用户真实配置在旧 `D:/my_project/wallpace/.wallspace.json`（`interval_minutes:100`、`auto_start:false`、图片目录 `D:/my_project/source picture`）。已备份 appdata 旧配置为 `config.json.bak.*`，并把该文件复制进 `%APPDATA%/.wallpace/config.json`，确保用户设置开机自启后不丢。运行 pytest 不会再污染（已验证全量跑完 appdata 不变）。
- **修过的两个 bug**：`gallery_page.py` 漏 import `QHBoxLayout`（导致所有 MainWindow 实例化失败）；`_apply_scan_results` 未刷新新整页画廊。
- **验证**：全量 `QT_QPA_PLATFORM=offscreen C:/Programs/Python/Python313/python.exe -m pytest -q` → **110 passed / 1 skipped**（基线保持）。offscreen 实跑：用真实配置构建 MainWindow，扫出 22 张图，`_gallery_page`/`_settings_page` 均存在，quit 正常。
- **待办**：commit（建议）+ push 到 origin/master（remote 内嵌 token，可联网 push）。
