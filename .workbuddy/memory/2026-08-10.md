# 2026-08-10

## 修复 Wallpace UI 卡死（添加文件夹/切换模式后未响应）

**真凶**：`src/app/window.py` `_refresh_dir_list()` 死循环 —— `while layout.count(): widget.deleteLater()`，deleteLater 不减少 count，主线程 100% CPU 永久挂起。修复：`takeAt(0)` + `deleteLater()`。

**附带修复**（工程师 寇豆码）：
- scan() 异步化：`_ScanJob`(QRunnable) + 专用单线程 QThreadPool，完成信号回主线程；`_scan_running`/`_scan_pending` 防重入；扫描期间禁用添加目录/模式切换入口，状态栏提示。
- 缩略图懒加载：首批 10 张（GALLERY_THUMB_BATCH），滚动距右端 ≤240px 追加下一批。

**验证**（QA 严过关）：103 passed / 1 skipped × 3 次；faulthandler 定向验证死循环消除、防重入生效、窗口销毁安全。

**遗留**：
- git 仓库有 corrupt object（image_loader.py blob 06e02132...），`git stash` 会失败，建议 `git fsck` 检查。
- [低] `_refresh_gallery_thumbnails` 清理旧缩略图只有 deleteLater 未 takeAt（非循环，不挂死，可后续统一）。
- [信息] image_loader 将全局 QThreadPool maxThreadCount 改为 4，影响全应用共享池。

**环境备忘**：本机沙箱默认 Python 无 pytest，用系统解释器 `C:\Programs\Python\Python313`；pytest 需用独立 `--basetemp` 规避沙箱 rmtree 拦截。
